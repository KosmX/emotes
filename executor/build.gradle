plugins {
    id 'java'
}

version project.mod_version

repositories {
    mavenCentral()
}

dependencies {
    compile project(':emotecraftCommon')
    implementation "com.google.code.gson:gson:2.8.0"//That package is compiled into MC
}